streamlit==1.37.1
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.1
joblib==1.4.2
pillow==10.4.0
# Note: TensorFlow removed to avoid installation failures on hosted platforms.
# If you need image-based prediction, add a TF-compatible wheel for the
# platform or use a separate inference service. To enable TF locally,
# install a compatible TensorFlow version in your environment.
streamlit==1.37.1
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.1
